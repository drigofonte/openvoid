{
  "name": "openvoid",
  "version": "0.0.0",
  "private": true,
  "description": "Open-source AI-assisted full-stack development platform — root workspace.",
  "license": "MIT",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "dev": "turbo run dev",
    "check-tools": "bash scripts/check-tools.sh"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  }
}
